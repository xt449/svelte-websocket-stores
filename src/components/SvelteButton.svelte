<!-- <script lang="ts">
	import { integers } from '../stores.ts';

	export let id: string;

    function randomize() {
        $integers[id] = Math.random() * 1000;
    }
</script>

<button {id} on:click={randomize}>Random</button> -->
